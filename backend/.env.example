# Application
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1

# ===========================================
# Supabase Configuration (必須)
# Supabase Dashboard > Project Settings > API から取得
# ===========================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_JWT_SECRET=your-jwt-secret-from-supabase-dashboard

# Database URL (Supabase PostgreSQL 接続文字列)
# Supabase Dashboard > Project Settings > Database > Connection string > URI
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project.supabase.co:5432/postgres

# ===========================================
# SSL Configuration
# ===========================================
# 開発環境でのSSL証明書検証スキップ（自己署名証明書用）
# "true" または "false" のみ有効。本番環境では無視される（常にtrue）
DB_SSL_REJECT_UNAUTHORIZED=true

# カスタムCA証明書（PEM文字列またはファイルパス）
# DB_SSL_CA_CERT=/path/to/ca-certificate.crt

# テスト環境でのみSSL無効化を許可（"true" または "false"）
# ALLOW_INSECURE_TEST_DB=false

# ===========================================
# Optional Settings
# ===========================================

# Logging
LOG_LEVEL=debug

# Reminder Settings
REMINDER_CHECK_INTERVAL=60000

# CORS Settings (comma-separated origins for production)
# Example: https://app.yourdomain.com,https://www.yourdomain.com
CORS_ORIGIN=

# ===========================================
# 旧設定（廃止予定 - 使用しないでください）
# ===========================================
# 以下の JWT 関連設定は Supabase Auth に移行したため不要です
# JWT_SECRET=xxx  # -> SUPABASE_JWT_SECRET を使用
# JWT_EXPIRES_IN=xxx  # -> Supabase Dashboard で設定
